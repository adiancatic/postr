{% extends 'main.html.twig' %}

{% block title %}Home{% endblock %}

{% block bodyContent %}
    <div class="home">
        <div class="row">
            <div class="container">
                <div class="row">

                    <div class="col-md-12 col-lg-8">
                        {{ include("post/create.html.twig") }}
                        {{ include("post/list.html.twig", {"posts": posts}) }}
                    </div>

                    <div class="col-lg-4">
                        {{ include("main/users-list.html.twig", {"usersListTitle": "Users you follow", "users": users}) }}
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
