<div class="follow-stats">
    <a href="#" class="stat follows">
        <strong>{{ follows|length }}</strong> Following
    </a>
    <a href="#" class="stat followers">
        <strong>{{ followers|length }}</strong> Followers
    </a>
</div>